#include <stdio.h>

/**
 * This is a simple code that adds two integers
 * main - entry point
 * Return: 0
 */

int main(void)
{
	int a, b, result;

	a = 1;
	b = 2;
	result = a + b;
	printf("%d\n", result);

	return (0);
}
