#include <stdio.h>

/**
 * This is a simple code that adds two integers
 * main - entry point
 * Return: result
 */

int main(void)
{
	int a, b, result;

	a = 1;
	b = 2;
	result = a + b;

	return (result);
}
